#include<stdio.h>
#include<stdlib.h>
#include<string.h>

char* retornaFinal(char* msg, int tamanho){
    for(int i =0)
}
int main(){

    char read[100] = {"NOMEs"}, *p = read+4, end;
    end = strcmp(read, );
    printf("%d", end);


    return 0;
}